#include"main.h"
/**
 * _abs - calculates the absolute value
 * @n: argument to be calculated
 * Return: the absolute value of n
 **/
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
	}
	return (n);
}

